import { getSupabaseClient } from './server/supabase'; async function test() { const client = getSupabaseClient(); const { data, error } = await client.from('wallet').select('*').limit(1); console.log('Data:', data); console.log('Error:', error); } test();  
